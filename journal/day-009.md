# Day 009 - Recovery from Common Git Mistakes

## Lesson - git reset, git revert, git commit --amend, git reflog

What happened:

- Practiced `git reset` (soft/mixed/hard), `git revert`, `git commit --amend`, and `git reflog` today.

Mistake made along the way (or things I found confusing):

- How to use each one confused me a bit at first, mostly because it had been a few days since the last session. Everything is clear now, just need more practice.
- My uncertainty was that it had been a few days and I'd forgotten some of it, but talking through it again helped me remember.

Key takeaway:

- My rule of thumb: reset/amend for cleaning up private history; revert for safely undoing shared history.
